const AUTH_COOKIE_NAME = "car_memory_auth_token";

const DEFAULT_COOKIE_LIFETIME = 1000 * 60 * 60; // 1 hours

const USER_DATA_HEADER_NAME = "user";

exports.module = {
  AUTH_COOKIE_NAME,
  DEFAULT_COOKIE_LIFETIME,
  USER_DATA_HEADER_NAME,
};
